apiVersion: v1
kind: Service
metadata:
  name: nexus-pass-vault
  namespace: default
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nexus-pass-vault
  ports:
    - name: http
      port: 80
      targetPort: http